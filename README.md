# Workload ABI

Workload ABI discovers operational breaking changes between container releases by executing them under identical conditions and comparing their observed runtime behavior.

> Initial implementation is under active development.
