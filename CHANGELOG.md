# Changelog

All notable changes to Workload ABI will be documented here.

## Unreleased

### Added

- initial `wabi` CLI with `record`, `diff`, and `compare` commands;
- versioned RuntimeGraph v1alpha1;
- Docker runtime recorder for process, network, filesystem, privilege, resource, and lifecycle surfaces;
- semantic runtime differ;
- Docker Compose compatibility solver;
- machine-readable JSON reports and CI failure thresholds;
- controlled scenario format and JSON Schema;
- end-to-end breaking-upgrade demo;
- CI, integration smoke test, and tagged binary release workflow.
